# Python Test Runner Draft

This area is reserved for future pytest-based orchestration of YAML conformance cases.

Suggested responsibilities:

- load YAML case files
- validate schema shape
- invoke the CC3D conformance runner
- compare outputs across implementations
