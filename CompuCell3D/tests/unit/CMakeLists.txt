# Draft CMake entry point for future unit and conformance tests.
#
# This file is intentionally not wired into the top-level build yet.
# The target structure below is the proposed starting point once the
# repository is ready to adopt GoogleTest/CTest for native plugin tests.
#
# Expected subdirectories:
#   cpp/     native C++ unit and component tests
#   python/  pytest-based orchestration and spec runners

# add_subdirectory(cpp)
